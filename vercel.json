{
  "buildCommand": "pnpm --filter web build",
  "installCommand": "pnpm install --frozen-lockfile",
  "github": {
    "enabled": true,
    "autoAlias": true,
    "silent": false
  },
  "projects": [
    {
      "name": "web",
      "rootDirectory": "apps/web"
    }
  ]
}
